name: cubit_feature_cubit
description: Create feature by folders
version: 1.0.0

environment:
  mason: "^0.1.1"

vars:
  name:
    type: string
    description: Enter the feature name [lowercase only]
    default: feature
    prompt: Enter the feature name [lowercase only]
